spring.application.name=Board7

# application.properties 파일안에서 명령 뒤에 # 주석쓰지말것 -중요
# Web page Setting : 시작주소를 /롤 설정
server.servlet.context-path=/  

#server 의 port 변경
server.port = 8080

# jsp 파일의 위치    src/main/webapp/WEB-INF/views/
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# JSP 수정시 서버 재시작없이 바로 적용할 수 있게 설정
server.servlet.jsp.init-parameters.development = true

# mybatis 설정 :  src/main/resources/   mapper.xml 경로 설정    /** : 하위폴더 포함 모두
mybatis.mapper-locations:classpath:mapper/**/*Mapper.xml

# datasource 설정
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=spring
spring.datasource.password=1234

# Mybatis SQL 로그 출력 설정 - error 아니더라도 여태까지 실행한 sql문 확인 가능함
# 스프링 부트는 기본 로깅 툴로 이미 SLF4F(logback)를 사요하고 있습니다.
# MyBatis 스프링 부트 스타터(mybatis-spring-boot-starter)는 이 설정을 자동으로 감지하므로 굳이 적지 않아도 알아서 slf4j
#mybatis.configuration.log-impl=org.apache.ibatis.logging.slf4j.Slf4jImpl

# SQL 실행 시 파라미터 로그를 출력하기 위한 설정 - 구동 로그까지 모두 출력되어 콘솔이 지저분 해짐
#logging.level.org.apache.ibatis=DEBUG

# SQL 실행 시 파라미터 로그를 출력하기 위한 설정
logging.level.com.green=DEBUG


## 파일업로드를 위해 추가된 부분
# file upload setting -no need library
spring.servlet.multipart.enabled=true
# 파일 저장 경로
spring.servlet.multipart.location=d:/dev/springboot/data/
# 업로드 될 파일당 최대 크기
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=1000MB
# properties 변수 
part1.upload-path=d:/dev/springboot/data/




